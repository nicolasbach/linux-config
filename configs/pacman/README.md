# Pacman Stuff

# Overview
- pacman.conf -> Custom Config for pacman
- secureboot_update.sh -> Script to copy boot relevant files to efi-Partition and sign them via sbctl
- custom hook which triggers the script after a kernel update
